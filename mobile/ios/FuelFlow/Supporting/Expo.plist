<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>EXUpdatesCheckOnLaunch</key>
    <string>ALWAYS</string>
    <!-- Disabled 2026-08-27: expo-updates aborted at launch on a fresh TestFlight install
         (SIGABRT, uncaught NSException on expo.controller.errorRecoveryQueue, ~600ms in).
         OTA is never invoked from JS and the channel was unsigned (FF-03). Re-enable ONLY
         together with code signing. This plist is what the Xcode Archive actually ships. -->
    <key>EXUpdatesEnabled</key>
    <false/>
    <key>EXUpdatesLaunchWaitMs</key>
    <integer>0</integer>
    <key>EXUpdatesRuntimeVersion</key>
    <string>1.0.0</string>
    <key>EXUpdatesURL</key>
    <string>https://u.expo.dev/37e2ff06-380f-417f-8d19-de2d988c9322</string>
  </dict>
</plist>