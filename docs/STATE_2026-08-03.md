# State Checkpoint — 2026-08-03

Return here to resume. Everything below is the exact state at this point.

## Git / Deploy

- Branch: `fix/ef-notracking-detached-updates`
- Local HEAD: `4e7e876b` (clean working tree) -> now has uncommitted refund work (see below)
- Deployed on Render: **`3cb7378`** (merge PR #275) — includes Monobank X-Key-Id key rotation + base64-of-PEM signature verification.
- Render service: `fuel-voucher-platform` -> `https://fuel-voucher-platform.onrender.com`

## Fraud fix status (see `docs/FRAUD_ANALYSIS.md`)

| WP | Item | Status |
|----|------|--------|
| WP-1 | Monobank webhook signature verification | Deployed + configured |
| WP-2 | Server-side pricing | Deployed |
| WP-3 | Admin audit (voucher update/delete/bulk/import) | Deployed |
| WP-7 | Automated backups | TODO (launch blocker) |
| WP-6 | Monitoring alerts | Partial (request/error logging only) |

### WP-1 config (Render Environment)

- `Monobank__PublicKey` = base64-of-PEM from `GET /api/merchant/pubkey` (set)
- `Monobank__Token` = sandbox token (set)
- `Monobank__WebhookUrl` = `https://fuel-voucher-platform.onrender.com/api/monobank/webhook` (set)
- `Monobank__Enabled` = true (via `appsettings.Development.json`)
- Render runs `ASPNETCORE_ENVIRONMENT=Development` -> Production fail-fast guard is NOT active.
- `SESSION_SECRET=change-me-in-production` -> replace before going live.

### WP-1 verification — still to confirm

TODO: run a fresh sandbox payment and confirm in app logs:
- `Processing Monobank webhook for invoice ...` then `Webhook processed: Order ... status ...` (verified, 200)
- OR `Monobank webhook rejected: invalid signature` (key mismatch) / `Error processing Monobank webhook` (500).

## Stuck order fix (data-only, in Supabase)

Order `ee30ad18-535c-4d17-b3d2-33867fb5b970` (July 28) is stuck at `PartiallyFulfilled`: 3 test orders x 2 vouchers, only 5 vouchers in stock. Recurring `process-fulfillments` job (`*/1 * * * *`, `Program.cs:96`) re-runs it every minute.

Planned SQL (Supabase SQL Editor): shrink the 10Lx2 line to 10Lx1 (`quantity=1`, `line_total=unit_price`), then `orders.price = SUM(line_total)`. Do NOT set `status='Fulfilled'` manually — the job derives it.

## Refund feature — IMPLEMENTED (uncommitted)

Full stack implemented on branch `fix/ef-notracking-detached-updates`, NOT yet committed/pushed/deployed.

### Backend

- New `Refund` entity + `refunds` table (unique `order_id`, snake_case) — `Features/Orders/SharedModels/Refund.cs` + `Configurations/RefundConfiguration.cs`. DbSet added to `ApplicationDbContext`. Migration **`20260803120600_AddRefunds`** (created, not yet applied to DB).
- `IMonobankClient.CancelInvoiceAsync(invoiceId, amountKopecks, extRef)` -> `POST /api/merchant/invoice/cancel` with `{invoiceId, extRef, amount}` (kopecks). Implemented in `MonobankClient.cs` + `MockMonobankClient`. New model `MonobankCancelResponse`.
- `RefundOrderCommandHandler` (`Features/Orders/RefundOrder/`): idempotent per order (unique order_id); amount = **server-computed** `(ordered units − fulfilled units) × unit_price × 100` (kopecks, matching `order.Price * 100` Monobank amount); statuses `NotFound` / `NotPayable` / `NothingToRefund` / `Processing` / `Failed`; audit via `ProviderEventService` (`RefundRequested` / `RefundFailed`). Registered in `ServiceSetup.cs`.
- Admin endpoint: `POST /api/admin/orders/{id}/refund` on `AdminOrderController` (body optional `{amountKopecks?, reason?}`; amount defaults to server-computed).
- **Auto-refund hook**: `FulfillmentService` now takes `RefundOrderCommandHandler`; fires `TryAutoRefundAsync` (best-effort, idempotent) whenever an order lands in `PartiallyFulfilled` — in `AssignVouchersToOrderAsync` and after mismatch-fix resets in `FixMismatchedFulfillmentsAsync`.
- Tests: new `RefundOrderCommandHandlerTests` (7 tests) + updated `ApiFulfillmentServiceTests` for new ctor. Full suite **294 passed**.

### Frontend (admin)

- `PurchaseType` interface fixed to match real DTO fields (`provider`, `fuelTypeId`, `createdAtUtc`, `voucherCount`, `quantity`, `lineItems`) — previously used nonexistent `stationName`/`fuelName`/`createdAt`, so the purchases tab was broken.
- Purchases table now shows `provider`, `fuelTypeId`, price in UAH, `vouchers fulfilled/total`, and a **Refund** button (yellow, per-row) with `window.confirm` + toast result (`refundPurchaseMutation`).
- i18n keys added in all 4 locales (en/uk/de/es): `table.vouchers`, `purchases.refund`, `purchases.refundConfirm`, `purchases.refundSuccess`, `purchases.refundFailed`.

### Verified

- `dotnet build` 0 errors; `dotnet test` 294 passed.
- `admin`: `npx tsc --noEmit` clean; `npm run build` succeeds.

## NEXT WORK ITEMS

1. **Commit + push** the refund work; merge to main; deploy to Render (needs migration — check how migrations are applied on Render, likely `DatabaseSetup` auto-applies).
2. **WP-1 verification**: run a fresh sandbox payment, confirm webhook logs.
3. **Stuck order**: apply Supabase SQL (see above).
4. **CancelList handling** (future): Monobank returns `{status:"processing"}` async; final result lands in the invoice's `cancelList`. Not yet polled/parsed — `refunds.status` stays `Processing` until we handle cancelList. Optional next iteration.

## Build / test commands

```powershell
dotnet build backend/src/FuelFlow.API/FuelFlow.API.csproj
dotnet test backend/tests/FuelFlow.UnitTests/FuelFlow.UnitTests.csproj
cd admin; npx tsc --noEmit; npm run build
```
